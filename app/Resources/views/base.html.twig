<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>

        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <style>
            #message_notice { font-weight: 500; text-align: center; }
        </style>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/style.css') }}" />
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-default">
            <div class="container-fluid">
                <div class="navbar-header">
                
                    <a class="navbar-brand" href="{{ path("homepage") }}">
                        <img class="m_logo_img" alt="marmiton.org" src="{{ asset('img/logo.png')}}">
                    </a>
                </div>
                <ul class="nav navbar-nav">
                    <li><a href="{{ path("homepage") }}">{{ 'nav-bar.home' |trans({}, 'FOSUserBundle') }}</a></li>
                    <li><a href="#">{{ 'nav-bar.recipe' |trans({}, 'FOSUserBundle') }}</a></li>
                    <li><a href="#">{{ 'nav-bar.news' |trans({}, 'FOSUserBundle') }}</a></li>
                    <li><a href="{{ path('fos_user_profile_edit') }}">{{ 'nav-bar.profile' |trans({}, 'FOSUserBundle') }}</a></li>
                    {#<li><a href="{{ path('fos_user_profile') }}">{{ 'nav-bar.profil' |trans({}, 'FOSUserBundle') }}</a></li>#}
                 </ul>
                <div class="nav navbar-nav navbar-right">
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <a href="{{ path('fos_user_security_logout') }}">
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                    </a>
                    {% else %}
                    <li><a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a></li>
                    {% endif %}
                </div>
            </div>
        </nav>
        <div id="message_notice">
            {%  for flashMessage in app.session.flashbag.get('notice') %}
                {{ flashMessage }}
            {% endfor %}
        </div>
        <div class="wrapper-body">
            <div class="container_body">
                {% block body %}{% endblock %}
            </div>
        </div>

        <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js">

        </script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous">

        </script>﻿
        {% block javascripts %}{% endblock %}
    </body>
</html>
